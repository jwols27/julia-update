## Script de atualização e manutenção

```shell
julia-update [OPÇÃO]
```

Opções disponíveis:

- `help` Mostra manual do comando.
- `reboot` Reinicia automaticamente no final.
- `skip` Executa o comando sem perguntar nada no final.
- `shutdown` Desliga automaticamente no final.